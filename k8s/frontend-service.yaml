apiVersion: v1
kind: Service
metadata:
  name: finconvert-frontend-service
spec:
  type: NodePort
  selector:
    app: finconvert-frontend
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    
    nodePort: 30080